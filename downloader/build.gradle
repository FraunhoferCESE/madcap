apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(path: ':backend', configuration: 'android-endpoints')
    compile 'com.google.inject:guice:3.0'
    compile 'com.google.apis:google-api-services-storage:v1-rev50-1.20.0'
}