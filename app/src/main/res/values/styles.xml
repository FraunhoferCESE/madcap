<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/madcap_main_light</item>
        <item name="colorPrimaryDark">@color/madcap_black</item>
        <item name="colorAccent">@color/madcap_main_light</item>
        <item name="android:colorForeground">@color/madcap_main_color</item>
    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--
        <item name="actionOverflowButtonStyle">@style/ActionButtonOverflowStyle</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        -->
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <!-- Activity with no Title -->
    <style name="ThemeOverlay.MyNoTitleActivity" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Dark Buttons -->
    <style name="ThemeOverlay.MyDarkButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/blue_grey_500</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="MadcapTheme" parent="AppTheme">
        <item name="colorPrimary">@color/madcap_main_light</item>
        <item name="colorPrimaryDark">@color/madcap_black</item>
        <item name="colorAccent">@color/madcap_main_light</item>
        <item name="android:colorForeground">@color/madcap_main_color</item>
    </style>

    <style name="MadcapTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MadcapTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="MadcapTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="logoutButtonText">Log out</style>

</resources>
